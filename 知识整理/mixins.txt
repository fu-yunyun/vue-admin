mixin 混入
混入 (mixins)： 是一种分发 Vue 组件中可复用功能的非常灵活的方式。混入对象可以包含任意组件选项。当组件使用混入对象时，所有混入对象的选项将被混入该组件本身的选项。

理解：类似于封装js函数，与其他js文件混合使用

混入的两种方式：
组件按需混入  全局混入

    注意：
    1、混入的data中,键名相同,则读取组件中的键名
    2、钩子：同名狗子函数，将混合为一个数组，因此都会被调用，混入 的钩子函数将在组件自身的钩子函数前调用
    3、值为对象的混入，如methods,components,选项会被合并，组件对象覆盖混入对象